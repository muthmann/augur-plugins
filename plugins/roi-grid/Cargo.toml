[package]
name = "augur-plugin-roi-grid"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "ROI Grid plugin for AugurRS — partitions the sensor around masked hotpixels and finds the largest clean capture regions."

[dependencies]
augur-core.workspace = true
egui.workspace = true
