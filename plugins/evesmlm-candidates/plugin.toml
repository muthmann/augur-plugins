name = "EVE Candidate Finding"
version = "0.2.0"
description = "Cluster raw eveSMLM events into per-emitter candidates using DBSCAN, eigenfeatures, or a frame-based fallback."
domain = "smlm"
library = "augur_plugin_evesmlm_candidates"
