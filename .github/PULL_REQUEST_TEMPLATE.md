## Summary

What does this PR add or change?

## Checklist

- [ ] `plugin.toml` is present and uses the runtime format (top-level fields, no `[plugin]` section)
- [ ] `README.md` describes configuration, published context keys, and dependencies
- [ ] `cargo fmt --all -- --check` passes
- [ ] `cargo build -p <crate-name> --release` succeeds
- [ ] `cargo test -p <crate-name>` passes (if tests exist)
- [ ] Tested in `augur-gui`: plugin appears in Plugin Manager, enable/disable works, settings render correctly

## Related issues

Closes #
